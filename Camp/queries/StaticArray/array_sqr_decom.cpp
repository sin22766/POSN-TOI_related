#include<bits/stdc++.h>

using namespace std;

int main() {
    int n,sqrN;
    scanf("%d", &n);
    sqrN = (int)sqrt(n);
    int arr[n],preCom[sqrN];

    int min = 1e9;
    for (int i = 0; i < n; ++i) {

    }
}